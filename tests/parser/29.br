struct Foo<T, var B> 
has(Bar, Copy)
{
    i32 a;
    fn foo(i32 a, i32 b) {
        do_work(a, b);
    }
    mt a() -> i32 {
        return self.a;
    }
    mt copy() -> Self {
        Self copy; // imagine there's a proper struct init here
        return copy;
    }
}

struct Bar has(Copy) {
    // ...
}
