fn foo(i32 x) -> i32 {
    return x * 2;
}

fn transform_i32(*fn(i32)->(i32) call, i32 x) -> i32 {
    return call(x);
}

fn main() {
    *fn(i32)->(i32) funcy = foo;
    [24] *fn(i32)->(i32) arr_of_funcs; // unsafe init, will be an error during later semantic passes
}
