fn main([&]str args) {
    char bing = '\t';
    str arg2 = args[1];
    str myStr = "This is my string.";
    foo(5);
}

fn convert([&]str strs) -> Vec<str> {
    return to_vec<str>(strs);
}

struct Dog {
    int age;
    str name;
    T itemOfChoice;
}
