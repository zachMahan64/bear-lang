// simple test

import "tests/hir/05.br";

fn main() -> i32 {
    compt i32 foo = 42;
    var fooey = Foo{.bar = 42, .barry = 12};
    var bar = Bar{.thingie = "12345"};
    return foo;
}

struct Foo {
    i32 bar;
    i32 barry;
}

struct Foo {
    i32 oops;
}

fn main() -> i32 {

}

mod animal {
    i32 dog;
    i32 cat;
    i32 dog;
}

mod animal {
    i32 cat;
    mod animal {
        i32 dog;
    }
}
