hid std..Arr<i32, sizeof(u8) * 4> thing;

pub var x = alignof(u8);

hid str my_str;

pub mut i32 y;

hid fn do_work(std..String item) -> std..String {
    item.transform().transform_again().foobar();
    return item;
}
