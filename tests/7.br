// mains with args
fn main(str[]& args) -> i32 {
    return 0;
}

// for example this must be true (although the user won't have to manually declare this)
mark NoMove requires !NoCopy; // this would be declared inside std

#[std..NoMove, std..NoDt]
struct Thing {
    i32 my_int
    fn new() -> Thing {
        Thing thing;
        thing.my_int = 42;
        return thing;
    }
}
