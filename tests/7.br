// mains with args
fn main(str[]& args) -> i32 {
    return 0;
}

#[std..NoCopy, std..NoDt]
struct Thing {
    i32 my_int
    fn new() -> Thing {
        Thing thing;
        thing.my_int = 42;
        return thing;
    }
}
