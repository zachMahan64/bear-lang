struct Foo
has(Bar, Copy)
<T, var B> 
{
    i32 a;
    fn foo(i32 a, i32 b) {
        do_work(a, b);
    }
    mt a() -> i32 {
        return self.a;
    }
    mt copy() -> Self {
        Self copy; // imagine there's a proper struct init here
        return copy;
    }
}
