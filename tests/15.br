fn annoying_funky_function(std..string my_str, box::i32 boxed_int) -> mut foo::<0x1000 * 4, std..string> {
    var x = get_what_we_need();
    return x;
}

fn more_demo() {

    // theoretical
    std..cpp_style_array<i32, 4096> arr;
    // nice 
    mut i32[8][16] other_arr;

    box::i32 thing;
    Vec<i32> veccy_thing;
    var other <- thing;
    other
        .transform()
        .make_nicer();
    return other;
}
