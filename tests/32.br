fn main([&]str args) -> i32 {
    return Foo..new().bar;
}

fn do_work(Foo foo) -> Foo {
    return magic_modify(&foo);
}

struct Foo {
    i32 bar;
    fn new() -> Self {
        return do_work(Foo{.bar = 64});
    }
}
