fn main (box::string[] args) {
    foo(5);
}

fn foo(int bar) -> bool {
    int what_up = 65536;
    whatup<<=4;// test comment
    int balls=333 + whatup;
    flt 1_agh; // intentional error btw
    return balls<222 + bar;
}
